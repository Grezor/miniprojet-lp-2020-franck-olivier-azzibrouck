{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<div class="container">

    <table class="table">
        <thead>
        <tr>
        <tr>
            <th>Libelle</th>
            <th>Date</th>
            <th>Taille</th>
            <th>actions</th>
        </tr>
        </tr>
        </thead>
        <tbody>
        {% for dossier in dossiers %}
            <tr>
                <td>
                    <a href="{{ path('dossier_show', {'id': dossier.id}) }}">

                            <span class="fas fa-fw fa-folder" style="font-size:40px;color:royalblue;"></span>
                           {{ dossier }}
                    </a>
                </td>
                <td>{{ dossier.libelle }}</td>
{#                <td>{{ dossier.date ? fichier.date|date('Y-m-d') : '' }}</td>#}
                <td>
                    <a href="{{ path('dossier_show', {'id': dossier.id}) }}">show</a>
                    <a href="{{ path('dossier_show', {'id': dossier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Votre repertoire es vide</td>
            </tr>
        {% endfor %}
        {% for fichier in fichiers %}
            <tr>
                <td>
                    <a href="{{ path('fichier_show', {'id': fichier.id}) }}">

                            <span class="fas fa-fw fa-file" style="font-size:40px;color:royalblue;"></span>
                           {{ fichier.id }}
                    </a>
                </td>
                <td>{{ fichier.libelle }}</td>
{#                <td>{{ dossier.date ? fichier.date|date('Y-m-d') : '' }}</td>#}
                <td>
                    <a href="{{ path('fichier_show', {'id': fichier.id}) }}">show</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Votre repertoire es vide</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
